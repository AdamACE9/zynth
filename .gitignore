# dependencies
node_modules/
.pnp
.pnp.js

# builds
dist/
build/
*.tsbuildinfo

# env / secrets
.env
.env.local
.env.*.local
!.env.example

# local SQLite database + WAL/SHM sidecars
*.sqlite
*.sqlite3
*.db
*.db-wal
*.db-shm
*.sqlite-wal
*.sqlite-shm
server/data/*.sqlite*
server/data/*.db*

# logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / os
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# vite
client/.vite/
.cache/

# firecrawl CLI local cache / scrape output — never commit scraped pages
.firecrawl/

# Claude Code machine-local permission cache — not shared
.claude/settings.local.json

# Vendored agent skill packs (npx skills add) — third-party, reinstallable,
# and three identical copies of the same 742 files. Not ours to version.
.agents/
.claude/skills/
agent/
skills-lock.json

# Markdown is working material — notes, scripts, run sheets, briefs.
# Only the repo front door is tracked.
*.md
!README.md
